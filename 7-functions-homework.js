// 5 problems with functions


/* ------------------------------------ example
 * Define a function that takes two numbers and returns their sum
 * then use console.log() to output result
 */
function add(first, second){
    let total = first + second;
    return total;
}
let answer = add(1, 3);
console.log(answer);
/* // we could have also rewritten the above
 * // to avoid the two variable initializations
 * function add(first, second){
 *     return first + total;
 * }
 * console.log(add(1, 3));
 */



/* ---------------------------------------- one
 * create a function that takes a Number
 * corresponding to a number of weeks
 * and returns the Number of minutes
 * in that length of time
 * example
 *     weekToMinutes(6);
 *     // 60480
 */



/* ---------------------------------------- two
 * create a function that takes a String
 * and returns the first Character
 *     firstChar('hola');
 *     // 'h'
 *     firstChar('que tal');
 *     // 'q'
 */



/* -------------------------------------- three
 * create a function that takes an Array of Numbers
 * and returns the Number sum of each
 *     sum([1,2,3]);
 *     // 6
 *     sum([0,0,0]);
 *     // 0
 */



/* --------------------------------------- four
 * create a function that takes a String
 * and returns a String in which each character is repeated once
 *     duplicate('hola');
 *     // 'hhoollaa'
 */



/* --------------------------------------- five
 * create a function that takes a String
 * and returns the Number count of vowels contained within it
 *     countVowels('Celebration');
 *     // 5
 *     countVowels('Palm');
 *     // 1
 */

